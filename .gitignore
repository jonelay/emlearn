.ipynb_checkpoints/
.coverage
*.swp
*.pyc
*.so
*.dll
*.egg-info/
melfilter*.png
# Figures rendered by the example scripts. Sphinx Gallery regenerates them
# into docs/auto_examples during the documentation build.
examples/example-*.png
# Timing results are measured on every run, since they only need a host
# compiler. The size results are a cached fallback and are tracked.
examples/tree_ensemble_perf_benchmark.csv
*.o
test/bench
.pytest_cache/
build/
dist/
prof/
tmp/
examples/xor_model.h
examples/classifiers
.vs

# Benchmark run outputs (regenerate with examples/mcu_benchmark/run_all.py)
examples/mcu_benchmark/runs/
# Dataset cache downloaded by examples/mcu_benchmark/datasets.py
/data/
