# Dependencies for MCU benchmark suite
# Install: uv pip install -r examples/mcu_benchmark/requirements.txt

# Core (required for all benchmarks)
setuptools>=41.0.0
numpy>=1.14.0
scikit-learn>=1.2.0
pandas>=1.3.0

# Figures
plotly>=5.0.0
matplotlib>=3.5.0
seaborn>=0.12.0

# MCU benchmarks (Renode/hardware)
west>=1.0.0
pytest-xdist>=3.0.0    # Parallel Renode tests
pyelftools>=0.29       # Zephyr ELF parsing
jsonschema>=4.0.0      # Zephyr board detection
